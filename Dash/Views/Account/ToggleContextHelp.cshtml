@using Dash.Resources;
@model Dash.Models.Help

<body-content>
    <breadcrumb>
        <breadcrumb-item label="@Core.ToggleHelp" controller="Account" action="ToggleContextHelp" is-active="true" />
    </breadcrumb>

    <partial name="_Toast" />

    <toast id="contextHelp" message="@(Model.Enabled ? Core.HelpEnabled : Core.HelpDisabled)" type="Info" allow-close="false" />
</body-content>
