@using Dash.Models
@model ReportFilter

<div id="filterOperatorPartial">
    @if (Model.ColumnId > 0)
    {
        <form-group-select for="OperatorId" options="Model.OperatorSelectListItems" toggle="@DataToggles.ContentReplace" url="@Url.Action("FilterCriteria")" params="#Id,#ColumnId,#OperatorId,#ReportId" />
        <partial name="_FilterCriteria" />
    }
</div>
